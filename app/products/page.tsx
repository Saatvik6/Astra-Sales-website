import Link from "next/link"; import { PageHero, SiteFooter, SiteHeader } from "../components"; import { productCategories } from "../site-data";
export const metadata={title:"Products | Astra Sales Surgical & Medical Portfolio"};
export default function Products(){return <main><SiteHeader/><PageHero eyebrow="Product portfolio" title="Specialised categories. One dependable partner." copy="Explore the principal categories represented in Astra Sales’ surgical and medical distribution portfolio."/><section className="listing section">{productCategories.map((p,i)=><Link className="listingCard" href={`/products/${p.slug}`} key={p.slug}><span>0{i+1}</span><h2>{p.title}</h2><p>{p.short}</p><b>View category →</b></Link>)}</section><section className="pageCta"><div><p className="kicker light">Need product guidance?</p><h2>Tell us what your team is looking for.</h2></div><Link className="button" href="/contact">Start an enquiry →</Link></section><SiteFooter/></main>}
