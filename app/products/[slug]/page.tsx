import Link from "next/link"; import { notFound } from "next/navigation"; import { PageHero, SiteFooter, SiteHeader } from "../../components"; import { productCategories } from "../../site-data";
export function generateStaticParams(){return productCategories.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=productCategories.find(x=>x.slug===slug);return {title:p?`${p.title} | Astra Sales`:"Products | Astra Sales"}}
export default async function ProductCategory({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=productCategories.find(x=>x.slug===slug);if(!p)notFound();return <main><SiteHeader/><PageHero eyebrow="Product category" title={p.title} copy={p.intro}/><section className="detailGrid section"><div><p className="kicker">Category overview</p><h2>Product support built around professional requirements.</h2><p>{p.short}</p><p>Contact Astra Sales for current brand availability, technical information, commercial enquiries and relevant product literature.</p></div><aside><h3>Areas of focus</h3>{p.focus.map((x,i)=><div key={x}><span>0{i+1}</span><b>{x}</b></div>)}</aside></section><section className="pageCta"><div><p className="kicker light">Enquire about {p.title}</p><h2>Request availability or product information.</h2></div><Link className="button" href={`/contact?category=${p.slug}`}>Send an enquiry →</Link></section><SiteFooter/></main>}
